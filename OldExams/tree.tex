
\documentclass{article}
\usepackage[margin=10mm]{geometry}
\pagestyle{empty}
\usepackage{tikz}
\usetikzlibrary{trees}
\begin{document}
\thispagestyle{empty}

\begin{tikzpicture}[
  grow'=down,
  level distance=20mm,
  level 1/.style={sibling distance=110mm},
  level 2/.style={sibling distance=55mm},
  level 3/.style={sibling distance=28mm},
  edge from parent/.style={draw},
  every node/.style={circle,draw,minimum size=9mm,inner sep=0.8mm,font=\small},
  edge from parent path={(\tikzparentnode.south) -- (\tikzchildnode.north)}
]
\node {$P_0$}
child { node {$P_1$}
  child { node {$P_3$}
    child { node {$P_7$} }
    child { node {$P_8$} }
  }
  child { node {$P_4$}
    child { node {$P_9$} }
    child { node {$P_{10}$} }
  }
}
child { node {$P_2$}
  child { node {$P_5$}
    child { node {$P_{11}$} }
    child { node {$P_{12}$} }
  }
  child { node {$P_6$}
    child { node {$P_{13}$} }
    child { node {$P_{14}$} }
  }
};
\end{tikzpicture}

\end{document}
